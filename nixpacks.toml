[phases.setup]
nixPkgs = ["python310", "pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python scripts/generate_sample_data.py", "python scripts/generate_preprocessed_analysis.py"]

[start]
cmd = "python app.py"
